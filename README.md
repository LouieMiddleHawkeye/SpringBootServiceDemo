# SpringBootServiceDemo

A basic Spring Boot Service application that uses a REST API controller to get a list of players from a postgreSQL database. Testing uses a memory based h2 database. On this branch the service, postgres, and pgAdmin are all run in a docker container (not entirely sure if implemented correctly though).
